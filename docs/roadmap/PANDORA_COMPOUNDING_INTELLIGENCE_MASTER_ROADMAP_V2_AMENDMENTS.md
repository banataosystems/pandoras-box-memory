# Master Roadmap v2 — live-evidence amendments

**This file supersedes the numbers in the roadmap, and does not replace it.**

`docs/roadmap/PANDORA_COMPOUNDING_INTELLIGENCE_MASTER_ROADMAP_V2.md` is a
byte-faithful versioned copy of the authoritative roadmap, GitHub issue #22.
`scripts/verify_compounding_roadmap_source.mjs` proves that equality against the
live issue on every run, which is what stops the versioned copy from drifting
away from the plan it claims to record.

That guarantee only holds if the copy stays byte-identical. Corrections
discovered after the issue was written therefore live **here**, dated, rather
than being edited into the copy. Where this file and the roadmap conflict, this
file is current and the roadmap statement is retained as dated history.

## Live-evidence correction — 2026-08-14

Provider-native readback records **68** live migration identities through
`20260813114649_remove_temporary_flutterflow_http_probe_20260813`. This
supersedes the issue body's dated **67**-through-`20260810115547` baseline while
preserving that earlier statement as historical evidence. The privacy-safe
readback is frozen in
`docs/capabilities/evidence/LIVE_SUPABASE_STATE_2026-08-14.json`.

Consequently:

- Phase 1 reconciles the **68** live migration identities, not 67, and still
  replays no history.
- The current verified baseline records **bounded positive-path** Memory
  health/search observations through the ProjectOS workload identity, with
  unresolved return-path reliability — not unqualified production verification.
- The canonical baseline is an exact **62-file** manifest plus historical
  deployment provenance, without original-archive parity and without a
  functional current-capability rollback.

## Original archive — blocked pending proof

The original archive bytes and the deterministic per-file manifest remain
**unavailable**. The 782-file count, archive size and hashes, source commit,
capability families, and the 113-test label are recorded **aggregate anchors
only**. No original file mapping, no parity claim, and no completion percentage
is valid until per-file evidence exists.

Treat the capsule as `blocked-pending-proof`: preserve every recovered byte, and
classify a file or capability only once its own evidence exists. Nothing may be
retired on the strength of the aggregate label alone.

## Deployment anchors — what they actually are

- `dpl_7CbTiMxMXQZjrLQDKchf455iBxi4` — observed live Memory deployment.
- `dpl_9EkwxicRPzigkvUis5m1qk644CrG` — recorded **historical** deployment. It
  lacks the current ProjectOS health/search routes, so it is READY historical
  evidence and **not** a functional rollback target.
- `Memory-main (4).zip`, source commit `7d4ec6cb30edb922024cf05f043807759d1fded7`
  — recorded full-source archive anchor, **bytes unavailable**.

## Additional open gaps

Beyond those named in the roadmap: original archive bytes and per-file evidence,
machine-gateway namespace isolation, and the unresolved MCP return-path
reliability window (7 successes / 4 failures) remain open.

## Next autonomous action, as amended

Continue Phase 1 with an evidence-first registry foundation across the recorded
original aggregate, the exact canonical baseline, and time-bounded live provider
state. Do not classify original files or claim completion until archive bytes and
per-file evidence are recovered. In parallel, implement and review the
highest-value safe source-only recovery candidates without production mutation.
